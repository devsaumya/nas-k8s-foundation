apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: runtime-apps
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/devsaumya/n100-services-runtime.git
      revision: HEAD
      directories:
      - path: apps/*
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/devsaumya/n100-services-runtime.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        createNamespace: true
